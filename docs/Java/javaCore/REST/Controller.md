# Controller

## Для чего нужен @Controller ?

Аннотация Controller используется для регистрации обработчиков http-запросов. Звено, которое принимает HTTP-запросы,
обрабатывает их и возвращает либо данные

## Самое важное

Это class-level аннотация, которая содержит аннотацию Component. Класс-контроллер выглядит как обычный POJO, с
методами-обработчиками и аннотациями.
Если добавить @ResponseBody - то вместо текстового представление которое за частую HTML страница - будем отправлять
текст, либо JSON XML.

# Шаблонизаторы

Шаблонизаторы — это инструменты, которые позволяют создавать HTML-шаблоны с динамическим содержимым, минимизируя
необходимость в повторении кода.

## Принцип работы

В файле описывается, что должен содержать HTML, в процессе кода генерируется HTML, по сути, страничка с
псевдопеременными. 

# RestController

По сути @Controller + RestController

## Интересные статьи

[Аннотации @Controller](https://javarush.com/quests/lectures/ru.javarush.java.spring.lecture.level07.lecture03)

